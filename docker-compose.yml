version: '3.8'

services:
  fishing:
    build:
      context: ./games/fishing
      dockerfile: Dockerfile
    container_name: fishing-game
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
